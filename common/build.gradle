import org.springframework.boot.gradle.tasks.bundling.BootJar

group 'org.document.common'
version '0.0.1-SNAPSHOT'

tasks.withType(BootJar).configureEach {
    enabled = false
}

dependencies {
}

test {
    useJUnitPlatform()
}